schema: spec-driven

context: |
  Project: vibepixel-academybabystepstech
  Detected stack: none detected
  Architecture: not detected
  Testing: no test runner detected
  Style: not detected

testing:
  strict_tdd: false
  test_runner: null
  test_command: null
  coverage_command: null
  linter: null
  type_checker: null
  formatter: null
  notes: No executable application stack was detected in the workspace.

rules:
  proposal:
    - Include rollback plan for risky changes
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
  tasks:
    - Group by phase, use hierarchical numbering
    - Keep tasks completable in one session
  apply:
    - Follow existing code patterns
    - Strict TDD is disabled until a test runner is introduced
  verify:
    - test_command: null
    - build_command: null
    - coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas
